To run my umlparser with the ball&socket test case: 

Unzip test.zip. 

If you run it on Mac/Linux: 
Open terminal, get into the umlparser¡¯s directory(should be in test folder), run 
$./umlparser ./testcase <output file name> 

If you run it on Windows: 
Open terminal, get into the target folder, run 
$java -jar ballnsocket-1.0-SNAPSHOT-jar-with-dependencies.jar ./testcase <output file  name>

The UML ball&socket diagram will be generated in the testcase folder. 